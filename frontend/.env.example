# REQUIRED in production: Set to your deployed backend URL (e.g., https://your-backend.onrender.com/api)
VITE_API_BASE_URL=

# Optional: used only for local Vite dev server proxy.
# Frontend dev server (/api) -> this backend origin.
VITE_DEV_API_TARGET=http://localhost:4000
