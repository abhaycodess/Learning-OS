# Production environment variables for Learning OS frontend
VITE_API_BASE_URL=https://learning-os.onrender.com/api

# Optional: used only for local Vite dev server proxy (not used in production)
VITE_DEV_API_TARGET=http://localhost:4000
